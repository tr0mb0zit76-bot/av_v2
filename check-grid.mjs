import * as agGrid from 'ag-grid-community';
console.log('Available exports:', Object.keys(agGrid));
console.log('Has AllCommunityModule:', !!agGrid.AllCommunityModule);
console.log('Has AllModules:', !!agGrid.AllModules);